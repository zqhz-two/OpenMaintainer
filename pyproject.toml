[build-system]
requires = []
build-backend = "backend"
backend-path = ["."]

[project]
name = "openmaintainer"
version = "0.1.0"
description = "GitHub 开源仓库维护 Agent Demo"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []
